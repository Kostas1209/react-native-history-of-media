import { StyleSheet } from "react-native";

const galeryStyles = StyleSheet.create({
    image : {
        marginTop : 10,
        marginLeft: 1,
        marginRight: 1
    }
})

export default galeryStyles