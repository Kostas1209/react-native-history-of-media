import {StyleSheet} from 'react-native';

const galeryStyles = StyleSheet.create({
  image: {
    marginTop: 10,
    maxWidth: 100,
    maxHeight: 100,
    resizeMode: 'contain',
  },
  imageContainer: {
    height: 100,
    width: 100,
  },
});

export default galeryStyles;
