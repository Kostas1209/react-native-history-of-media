import { StyleSheet } from "react-native"

const styles = StyleSheet.create ({
    image:{
        maxHeight: "100%",
        maxWidth: "100%",
        alignSelf: "center",
        resizeMode: "contain",
    }
})


export default styles;