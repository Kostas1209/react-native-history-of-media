import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    resizeMode: 'contain',
    width: 100,
    height: 100,
    position: 'absolute',
    left: '75%',
    top: '40%',
  },
});

export default styles;
