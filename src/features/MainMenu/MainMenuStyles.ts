import { StyleSheet } from 'react-native'

const styles = StyleSheet.create ({
    text:{
        textAlign: "left"
    },
    container :
    {
        top: "40%", 
        left: "30%"
    }
})

export default styles