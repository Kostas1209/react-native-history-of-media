import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  image: {
    alignSelf: 'center',
    marginTop: 5,
    marginBottom: 5,
    maxHeight: 40,
    maxWidth: 40,
    resizeMode: 'contain',
  },
  itemContainer: {},
});
